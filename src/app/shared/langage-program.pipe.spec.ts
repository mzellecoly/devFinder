import { LangageProgramPipe } from './langage-program.pipe';

describe('LangageProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new LangageProgramPipe();
    expect(pipe).toBeTruthy();
  });
});
